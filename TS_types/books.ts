export type TBookAdd = {
    title: string,
    author: string
}

export type TBookPutch = {
    title?: string,
    author?: string
}

export type TBooktGet = {
    id: string,
    title: string,
    author: string
}

